{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs + events (VU meters).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
