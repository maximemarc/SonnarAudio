// Conventional Commits — enforced by the commit-msg hook (see .husky/) and
// by CI on every pull request. Rules and examples: CONTRIBUTING.md.
export default {
  extends: ["@commitlint/config-conventional"],
};
